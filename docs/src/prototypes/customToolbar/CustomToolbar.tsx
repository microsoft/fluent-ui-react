import * as React from 'react'

export interface CustomToolbarProps {}

const CustomToolbar: React.FunctionComponent<CustomToolbarProps> = props => (
  <div {...props}>CustomToolbar</div>
)

export default CustomToolbar
