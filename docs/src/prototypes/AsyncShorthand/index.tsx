import * as React from 'react'
import { PrototypeSection, ComponentPrototype } from '../Protoypes'
import AsyncShorthand from './AsyncShorthand'

export default () => (
  <PrototypeSection title="Chat Messages Async Shorthand">
    <ComponentPrototype>
      <AsyncShorthand />
    </ComponentPrototype>
  </PrototypeSection>
)
