import * as React from 'react'
import { PrototypeSection, ComponentPrototype } from '../Prototypes'
import Tokens from './Tokens'

export default () => (
  <PrototypeSection title="Tokens">
    <ComponentPrototype
      title="Tokens"
      description="Prototyping token management patterns with Fluent design orgs."
    >
      <Tokens />
    </ComponentPrototype>
  </PrototypeSection>
)
