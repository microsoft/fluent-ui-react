'use strict'

Object.defineProperty(exports, '__esModule', {
  value: true,
})

var _typeof =
  typeof Symbol === 'function' && typeof Symbol.iterator === 'symbol'
    ? function(obj) {
        return typeof obj
      }
    : function(obj) {
        return obj &&
          typeof Symbol === 'function' &&
          obj.constructor === Symbol &&
          obj !== Symbol.prototype
          ? 'symbol'
          : typeof obj
      }

exports.default = generateUniqueHash

var _stringHash = require('string-hash')

var _stringHash2 = _interopRequireDefault(_stringHash)

function _interopRequireDefault(obj) {
  return obj && obj.__esModule ? obj : { default: obj }
}

var collisionBuffer = {}

function generateUniqueHash(reference) {
  var result =
    (typeof reference === 'undefined' ? 'undefined' : _typeof(reference)) === 'object'
      ? JSON.stringify(reference)
      : reference
  var prevResult = void 0

  do {
    prevResult = result
    result = (0, _stringHash2.default)(prevResult).toString(36)
  } while (collisionBuffer[result] && collisionBuffer[result] !== prevResult)

  collisionBuffer[result] = prevResult

  return '_' + result
}
