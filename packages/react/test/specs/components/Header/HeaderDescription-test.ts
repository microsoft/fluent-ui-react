import { isConformant } from '../../commonTests'

import HeaderDescription from '@fluentui/react/src/components/Header/HeaderDescription'

describe('HeaderDescription', () => {
  isConformant(HeaderDescription)
})
