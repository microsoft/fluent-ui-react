import { ICSSInJSStyle } from '../../../types'

const treeListItemStyles = {
  root: (): ICSSInJSStyle => ({
    listStyleType: 'none',
    padding: '0 0 0 .5rem',
  }),
}

export default treeListItemStyles
