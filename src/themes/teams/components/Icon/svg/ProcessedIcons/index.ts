import { TeamsSvgIconSpec } from '../types'

import accept from './icons-accept'
import addParticipants from './icons-add-participant'
import add from './icons-add'

export default {
  accept,
  addParticipants,
  add,
} as { [iconName: string]: TeamsSvgIconSpec }
