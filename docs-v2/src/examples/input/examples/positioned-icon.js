import React from "react"
import {Input} from "@stardust-ui/react"

// description: An input's icon can be positioned at the "start" or "end" (default) of the input.
export default (
  <Input icon="search" iconPosition="start" placeholder="Search..." />
)
