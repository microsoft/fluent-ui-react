import React from "react"
import {Input} from "@stardust-ui/react"

// description: An input can be rendered in-line with other content.
export default (
  <>
    There's an inline input
    <Input inline style={{margin: "0 1rem"}} />
    inside of this content
  </>
)
