import React from "react"
import {Input} from "@stardust-ui/react"

// description: A clearable input renders an "X" that allows a user to clear the input's current value.
export default <Input defaultValue="You can clear me" clearable />
