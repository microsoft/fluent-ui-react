import React from "react"
import {Text} from "@stardust-ui/react"

// description: A piece of text can appear disabled, indicating that it (or nearby content) can't be interacted with.
export default (
  <Text disabled content="I'm disabled, so you can't interact with me." />
)
