import React from "react"
import {HierarchicalTree} from "@stardust-ui/react"

// description: An exclusive tree allows only one branch to be open at each level. Opening a collapse branch will automatically close the open one.
export default undefined
