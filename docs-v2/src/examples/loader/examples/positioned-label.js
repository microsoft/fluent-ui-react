import React from "react"
import {Loader} from "@stardust-ui/react"

// description: A loader's label can be positioned "before", "after", "above", or "below" (default) the spinner.
export default (
  <Loader label="Label positioned above loader" labelPosition="above" />
)
