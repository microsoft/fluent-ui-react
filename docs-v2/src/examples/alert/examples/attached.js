import React from "react"
import {Alert} from "@stardust-ui/react"

// description: An alert can be attached to a nearby element to provide additional information, warnings, or errors.
export default (
  <div>
    <Alert />
    <Input fluid placeholder="Name" style={{width: "200px"}} />
  </div>
)
