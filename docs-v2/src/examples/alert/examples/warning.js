import React from "react"
import {Alert} from "@stardust-ui/react"

// description: A "warning" alert warns the user, perhaps to prevent common mistakes or indicate a potential problem.
export default <Alert warning content='This is a "warning" alert' />
