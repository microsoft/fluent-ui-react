export default (
  <Grid
    columns={3}
    content={[
      <div key={1} style={{padding: "1rem", border: "1px dashed #000"}}>
        1
      </div>,
      <div key={2} style={{padding: "1rem", border: "1px dashed #000"}}>
        2
      </div>,
      <div key={3} style={{padding: "1rem", border: "1px dashed #000"}}>
        3
      </div>
    ]}
  />
)
